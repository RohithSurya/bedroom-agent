name: sleep_mode_no_presence
description: "Presence false -> deny + TTS"

initial_state:
  presence: false
  guest_mode: false

steps:
  - request:
      intent: sleep_mode
      args: {}
    expect:
      decision: deny
      action_tools:
        - tts.say
