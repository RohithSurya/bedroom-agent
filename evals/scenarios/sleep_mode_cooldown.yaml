name: sleep_mode_cooldown
description: "Call sleep_mode twice; second should be denied due to cooldown"

initial_state:
  presence: true
  guest_mode: false

steps:
  - request:
      intent: sleep_mode
      args: {}
    expect:
      decision: allow
      action_tools:
        - light.set
        - tts.say

  - request:
      intent: sleep_mode
      args: {}
    expect:
      decision: deny
      action_tools:
        - tts.say
