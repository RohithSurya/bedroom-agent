name: sleep_mode_basic
description: "Presence true + guest_mode false -> allow + sleep actions + TTS"

initial_state:
  presence: true
  guest_mode: false

steps:
  - request:
      intent: sleep_mode
      args: {}
    expect:
      decision: allow
      action_tools:
        - light.set
        - tts.say
